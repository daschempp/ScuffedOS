# Scuffed OS backend
fastapi>=0.115.0
uvicorn>=0.32.0
pydantic-settings>=2.6.0
sqlalchemy>=2.0.44
alembic>=1.16
psycopg[binary,pool]>=3.2
anthropic>=0.69
# Memory engine (M2): self-hosted Mem0 — Claude extraction, OpenAI embedder
# (text-embedding-3-small), vectors in pgvector. Needs OPENAI_API_KEY.
mem0ai>=2.0
openai>=1.0
# Local domains (M3): RRULE recurrence, multipart uploads, food-DB lookups.
python-dateutil>=2.9
python-multipart>=0.0.20
httpx>=0.27
# Secrets vault (M8 Slice 2): AES-256-GCM + HKDF via cryptography; keyring
# wraps the single 32-byte vault key in one OS-keychain item in the packaged
# app (degrades to a file-only key where no backend exists, e.g. CI).
cryptography>=43
keyring>=25
# Optional, for `uvicorn --reload` + faster event loop (compiled deps):
#   pip install "uvicorn[standard]"
# Identity canonicalization (M10 s1): E.164 phone parsing, pure-Python/offline.
phonenumberslite>=9.0

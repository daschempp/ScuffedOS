Checkbox with a green check — the task-manager workhorse.

```jsx
<Checkbox defaultChecked label="Pay rent" strikeWhenChecked />
<Checkbox label="Call the dentist" />
```

Set `strikeWhenChecked` to fade + strike the label when complete (task-list behavior). Controlled or uncontrolled like a native checkbox.

Round avatar — image or auto initials with a warm tint.

```jsx
<Avatar name="Sam Rivera" size="md" />
<Avatar src="/me.jpg" name="Sam" size="lg" />
```

Sizes `xs`/`sm`/`md`/`lg`. Without `src`, shows up to two initials on a tint (`green`, `clay`, `honey`, `sky`, `plum`).

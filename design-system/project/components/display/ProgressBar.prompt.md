Horizontal progress bar for budgets, goals, and macros.

```jsx
<ProgressBar label="Groceries" meta="$320 / $400" value={320} max={400} color="clay" />
```

`color`: `green`/`clay`/`honey`/`sky`/`plum`. `size`: `sm`/`md`/`lg`. Sunken track, rounded fill, animates on value change.
